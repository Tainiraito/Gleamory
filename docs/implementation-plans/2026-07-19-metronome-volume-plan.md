# 节拍器 v1.2.0 音量控制实施计划

## 实施状态

- [x] 建立独立 `feat/metronome-volume` worktree，避免混入 Furigana 与字体分支提交。
- [x] 增加音量归一化、读取、保存和异常回退。
- [x] 为节拍器建立共享主 GainNode，并统一主拍、细分拍和试听输出。
- [x] 在播放按钮右侧增加响应式音量按钮、滑杆、百分比与关闭交互。
- [x] 补充存储、Hook、组件和共享 Slider 的自动测试。
- [x] 同步项目版本、README、CHANGELOG、需求和设计记录，不更新里程碑时间线。
- [x] 通过目标测试、全量测试、Lint、生产构建和 `git diff --check`。
- [ ] 合入字体分支后运行新增的 `npm run check:fonts`。
- [ ] 在可用浏览器环境完成桌面与 320px 窄屏截图和实际声音验证；当前内置浏览器受 WSL 路径桥接阻断，仓库也未安装 Playwright。

## 实施约束

- 不修改 `MetronomeConfig`，音量作为设备侧播放偏好独立保存。
- 不新增依赖，不实现调研路线图中的其他产品能力。
- 推送前合入最新 `origin/main`，处理字体分支对节拍器样式的少量重叠修改并重新验证。

# Changelog

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added

- 新增音高检测 v0.1.0：支持麦克风与电脑音频实时检测、本地音频上传分析、音高曲线交互和音轨分离结果接续分析。
- 音高检测纵轴音名支持点击或键盘播放本地钢琴参照音。
- 新增指板音训练 v0.3.0：五类可配置测验、单题计时与部分准确率、可交互指板地图和吉他采样音色。
- 指板音训练新增玫瑰木、枫木、乌木和高对比练习四套可持久化外观预设。
- 节拍器支持每拍独立细分（1/2/3/4），长按拍点可设置不同 subdivisions。
- 节拍网格可视化细分 tick（小圆跟随主拍圆）。
- 全局细分选择器（♩/♪/♩³/♬/自定义），一键统一所有拍。
- 细分 tick 独立高亮，播放时逐个亮起。
- 数字输入框组件 `ClampedNumberInput`，失焦/Enter 时才 clamp，避免输入多位数被截断。

### Fixed

- 修复音高检测错误提示超出侧栏，以及共享滑块方向选择器失效造成音量、播放进度轨道几乎不可见的问题。
- 音高检测严格遵守置信度阈值，并支持分析短于默认检测帧的非空音频片段。
- 音高检测页面卸载时释放媒体流、动画帧、对象 URL 和实时 AudioContext。
- 修复实时录音回放状态提前切换、曲线点击无法可靠播放、升降号音名被截断和低频纵轴标签重叠。
- Spleeter 改为从固定版本远程源下载，保留镜像和本地回退，并移除仓库中约 75 MiB 的重复分发权重。
- 移除与当前 `onnxruntime-web` 版本不匹配的手工 WASM 副本，改由 Vite 从锁定依赖生成唯一运行时资源。
- 清理公开模型文档中的个人路径、局域网代理和过期部署状态，并删除一份未引用的重复截图。
- 指板音训练在浏览器存储不可用时不再中断页面，并为未保存状态提供提示。
- 完善指板练习嵌套术语浮层的键盘交互，并在离开页面时释放吉他音频资源。
- 路由页面卸载时恢复之前的网页标题。
- 时间线按实际日期降序排列，无效日期稳定排在最后。
- 钢琴页面卸载时释放活动音符并关闭 Web Audio 上下文。
- 修复抽卡卡牌网格的 React Hook 依赖警告。
- 补充实际存在的 Open Graph 和 Twitter 分享图。

### Changed

- 音高检测麦克风默认启用浏览器降噪、70–1600 Hz 检测滤波、噪声门和短断点稳定处理，并提供可关闭开关。
- 音高曲线纵轴改为对数音高比例，使相同音程保持一致的视觉距离。
- 指板今日练习改为手动生成单题，只显示本题用时和支持部分得分的准确率；移除每日记录、历史弹窗、热力图及练习历史持久化。
- 指板地图的当前位置详情移至指板下方并独立于音名淡出；今日练习改为答题结束后再显示最终用时。
- 指板使用十二平均律递减品距，并区分六根弦的粗细、缠弦/钢弦、琴枕、品丝和单/双品位标记。
- 抽卡页面改为复用 `src/lib/gacha.ts` 中经过测试的文本处理、洗牌和持久化逻辑。
- 抽卡测试改为覆盖当前生产状态结构，不再验证已废弃模型。
- 非首页路由使用 `React.lazy` 独立分包。
- 思源宋体从完整 OTF 改为子集化 WOFF2。
- 项目封面从 PNG 改为压缩 WebP，并清理未引用资源。
- README、工程说明和需求文档同步为当前 React 架构。

### CI

- GitHub Pages 部署前依次执行测试、ESLint 和生产构建。

## [3.0.0] - 2026-05-18

### Changed

- 从 Vue 3 迁移到 React 19 和 TypeScript。
- 从 Tailwind CSS 3 迁移到 Tailwind CSS 4 CSS-first 配置。
- 使用 Framer Motion 统一页面动画。
- 首页重构为暖纸杂志布局，增加实时日历和每日诗句。
- 保留 GitHub Pages 自动部署和自定义域名。

### Added

- 翻牌抽卡、极简钢琴和节拍器。
- 网易云封面提取与 Pixiv 插画下载插件说明页。
- 共享 SiteHeader、项目页标题、返回页脚和插件详情模板。

更早版本的详细演进记录保留在 Git 提交历史中。

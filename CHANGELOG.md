# Changelog

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added

- 节拍器 v1.2.0 在播放按钮旁新增音量控制，支持 0–100% 实时调节、静音状态和本地记忆。
- 新增音高检测 v0.1.0：支持麦克风与电脑音频实时检测、本地音频上传分析、音高曲线交互和音轨分离结果接续分析。
- 音高检测纵轴音名支持点击或键盘播放本地钢琴参照音。
- 音高检测支持枚举多个音频输入，并在开始前手动选择指定麦克风。
- 音高检测支持用空格键快速开始或暂停实时录制。
- 音高检测上传分析支持用空格键播放或暂停音频。
- 音高曲线增加双手柄时间轴范围条：左右手柄调整可视边界，中段拖动平移范围，并与曲线滚轮缩放实时联动。
- 音高曲线增加播放位置跟随开关：启用后视口随播放线平移，手动拖动曲线或时间轴范围后自动退出跟随。
- 新增指板音训练 v0.3.0：五类可配置测验、单题计时与部分准确率、可交互指板地图和吉他采样音色。
- 指板音训练新增玫瑰木、枫木、乌木和高对比练习四套可持久化外观预设。
- 节拍器支持每拍独立细分（1/2/3/4），长按拍点可设置不同 subdivisions。
- 节拍网格可视化细分 tick（小圆跟随主拍圆）。
- 全局细分选择器（♩/♪/♩³/♬/自定义），一键统一所有拍。
- 细分 tick 独立高亮，播放时逐个亮起。
- 数字输入框组件 `ClampedNumberInput`，失焦/Enter 时才 clamp，避免输入多位数被截断。

### Fixed

- 重构为本地“表达 / 产品 / 数据”三层字体体系：思源宋体负责标题与诗词，思源黑体负责正文与控件，Source Code Pro 负责数字与代码；禁止合成字重和中文斜体，补齐动态中文 GB2312 覆盖，改善部分设备上同句文字清晰度不一致的问题。
- 修复点击页面按钮后空格键重复激活该按钮、与实时录制开始/暂停快捷键冲突的问题。
- 音高检测在媒体采集 API 缺失或页面不是安全上下文时改为显示 HTTPS/浏览器兼容性提示，不再抛出读取未定义方法的错误。
- 音高检测上传人声使用更合适的离线检测门槛并连接短清音间隔，播放头改为动画帧同步，减少曲线碎片和回放定位线跳动。
- 修复音高检测上传侧栏过窄、长文件名与重复日志挤压播放器的问题。
- 修复音高检测工作台顶栏与正文侧栏分界线宽度不一致的问题。
- 修复上传音频缩放后以最后一个有效音高点收缩时间轴，导致尾部静音区无法继续跟随的问题。
- 实时检测和实时录音回放改为与上传播放一致的动画帧游标更新，减少播放竖线与视口平移的跳动；从回放继续采集时会停止旧录音、退出播放跟随并回到最新采集位置。
- 播放跟随开启时将游标和视口合并到同一动画帧更新，忽略播放期间低频媒体事件的重复写入，并缓存曲线路径计算，减少长音频跟随时的掉帧和回摆。
- 音高检测改进基频识别、实时平滑和短丢帧桥接，减少强谐波误判、曲线抖动与频繁断线。
- 修复音高检测把多次实时录音直接拼接为无效 WebM，导致继续采集后的内容无法回放；曲线跨大音程跳点时不再绘制误导性的竖直连线，并修正空闲读数与音分刻度对齐。
- 修复音高检测错误提示超出侧栏，以及共享滑块方向选择器失效造成音量、播放进度轨道几乎不可见的问题。
- 音高检测严格遵守置信度阈值，并支持分析短于默认检测帧的非空音频片段。
- 音高检测页面卸载时释放媒体流、动画帧、对象 URL 和实时 AudioContext。
- 修复实时录音回放状态提前切换、曲线点击无法可靠播放、升降号音名被截断和低频纵轴标签重叠。
- Spleeter 改为从固定版本远程源下载，保留镜像和本地回退，并移除仓库中约 75 MiB 的重复分发权重。
- 移除与当前 `onnxruntime-web` 版本不匹配的手工 WASM 副本，改由 Vite 从锁定依赖生成唯一运行时资源。
- 清理公开模型文档中的个人路径、局域网代理和过期部署状态，并删除一份未引用的重复截图。
- 指板音训练在浏览器存储不可用时不再中断页面，并为未保存状态提供提示。
- 完善指板练习嵌套术语浮层的键盘交互，并在离开页面时释放吉他音频资源。
- 路由页面卸载时恢复之前的网页标题。
- 时间线按实际日期降序排列，无效日期稳定排在最后。
- 钢琴页面卸载时释放活动音符并关闭 Web Audio 上下文。
- 修复抽卡卡牌网格的 React Hook 依赖警告。
- 补充实际存在的 Open Graph 和 Twitter 分享图。

### Changed

- 全站页面与顶部站点标识收敛为 90rem 标准容器和 100rem 宽屏工作台两档；首页轮播改为常驻图层交叉淡化，功能页标题简介恢复统一中轴。
- 移除霞鹜文楷外部请求、系统字体主来源和失真的 `font-kai` token；统一 11–60px 字阶与 400/500/600 字重，并让字体栅格化遵循浏览器和操作系统默认策略。
- 新增字体许可证、第三方声明、来源与覆盖 manifest，以及 CI 字符覆盖、哈希和体积门禁；四份中文字重统一覆盖 GB2312，字体资产总计控制在 7.5 MB 以内。
- 音高检测因新增多项局部功能由 v0.2.0 升级至 v0.3.0；项目元数据版本统一使用 `v主版本.次版本.修订版本` 三段格式。
- 音高检测因上传轨迹、回放交互和线上兼容提示更新由 v0.3.0 升级至 v0.4.0。
- 音高检测因新增播放位置跟随能力由 v0.4.0 升级至 v0.5.0。
- 音高检测实时采样频率提高到约 25 次/秒；麦克风采集关闭面向语音的回声消除、自动增益与浏览器降噪，环境降噪开关改用本地带通滤波，避免持续乐器音被切断。
- 实时音高曲线在首屏填满后改为居中跟随，最新输入点保持在 X 轴中部并同时展示前后时间上下文。
- 首页主视觉改为跨满内容容器的浅暖纸信息区和 16:9 图片视窗，与右侧栏外沿对齐。
- 轮播控制收敛为图片外的唯一一组页码、上一张、暂停/继续和下一张，移除遮挡图片的覆盖按钮与重复分段切换。
- 首页主视觉、项目目录和右侧栏统一限制在居中的 90rem 内容容器内，改善高分辨率屏幕的阅读跨度。
- 首页分类栏改用卷次章节标记，项目行移除重复序号；右侧栏统一模块内边距、标题基线与分隔线。
- “流光忆庭”恢复额外记录展开与收起的过渡动效，并继续遵循系统减少动态效果设置。
- “流光忆庭”收敛为项目里程碑，只保留核心能力大改与阶段事件，并移除实现细节和普通界面调整记录。
- 首页分类更名为“弦歌有声、丹青拾光、浮生半日”，移除重复的项目计数；月历与每日诗词改为共享边线的开放式“今日一页”。
- 首页分类项目由矩形卡片网格改为一行一个的开放式列表，并增强全站文本框选的视觉对比。
- 首页主视觉简介精简为“汇聚所有微光般的创意与灵感”。
- 首页自链接主卡片改为三图主视觉轮播，只保留站点标题与简介，并支持自动播放、手动切换、暂停和减少动态效果。
- 首页普通项目按“弦歌有声、丹青拾光、浮生半日”分类展示，移除用于补齐网格的占位项，并让交互动效遵循系统的减少动态效果设置。
- 音高检测麦克风默认启用 55–2000 Hz 本地检测滤波、噪声门和短断点稳定处理，并提供可关闭开关。
- 音高曲线纵轴改为对数音高比例，使相同音程保持一致的视觉距离。
- 指板今日练习改为手动生成单题，只显示本题用时和支持部分得分的准确率；移除每日记录、历史弹窗、热力图及练习历史持久化。
- 指板地图的当前位置详情移至指板下方并独立于音名淡出；今日练习改为答题结束后再显示最终用时。
- 指板使用十二平均律递减品距，并区分六根弦的粗细、缠弦/钢弦、琴枕、品丝和单/双品位标记。
- 抽卡页面改为复用 `src/lib/gacha.ts` 中经过测试的文本处理、洗牌和持久化逻辑。
- 抽卡测试改为覆盖当前生产状态结构，不再验证已废弃模型。
- 非首页路由使用 `React.lazy` 独立分包。
- 思源宋体从完整 OTF 改为子集化 WOFF2。
- 项目封面从 PNG 改为压缩 WebP，并清理未引用资源。
- README、工程说明和需求文档同步为当前 React 架构。

### CI

- GitHub Pages 部署前依次执行测试、ESLint 和生产构建。

## [3.0.0] - 2026-05-18

### Changed

- 从 Vue 3 迁移到 React 19 和 TypeScript。
- 从 Tailwind CSS 3 迁移到 Tailwind CSS 4 CSS-first 配置。
- 使用 Framer Motion 统一页面动画。
- 首页重构为暖纸杂志布局，增加实时日历和每日诗句。
- 保留 GitHub Pages 自动部署和自定义域名。

### Added

- 翻牌抽卡、极简钢琴和节拍器。
- 网易云封面提取与 Pixiv 插画下载插件说明页。
- 共享 SiteHeader、项目页标题、返回页脚和插件详情模板。

更早版本的详细演进记录保留在 Git 提交历史中。
